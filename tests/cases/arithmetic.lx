fn topla(a: i32, b: i32) -> i32 {
      return a + b;
  }

  fn main() -> i32 {
      let x: i32 = 10;
      let y: i32 = 20;
      let z: i32 = topla(x, y);
      let big: i64 = 9000000000;
      let neg: i32 = -5;
      let cast: i64 = x as i64;
      return z;
  }