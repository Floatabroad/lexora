[package]
name = "lexora"
version = "0.1.0"
edition = "2024"


[features]
default = ["string-ir"]
string-ir = []
inkwell = ["dep:inkwell"]

[dependencies]
bumpalo = "3"
inkwell = { version = "0.9.0", features = ["llvm22-1"], optional = true }