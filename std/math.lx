fn abs(x: i32) -> i32 {
    if x < 0 {
        return -x;
    }
    return x;
}

fn max(x: i32, y: i32) -> i32 {
    if x > y {
        return x;
    }
    return y;
}

fn min(x: i32, y: i32) -> i32 {
    if x < y {
        return x;
    }
    return y;
}